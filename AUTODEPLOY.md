# 🚀 Автоматичний Деплой

## Швидкий старт

### 1. Завантажте код на сервер

```bash
# З вашого комп'ютера (Windows)
scp -r C:\Users\tramb\OneDrive\Desktop\teacherAgentTest\TeacherAgentTest root@157.230.104.129:/var/www/teacher-agent
```

### 2. Підключіться до сервера

```bash
ssh root@157.230.104.129
```

### 3. Запустіть скрипт деплою

```bash
cd /var/www/teacher-agent
sudo python3 deploy.py
```

### 4. Відповідайте на питання скрипта

Скрипт запитає:
- ✅ GitHub repo (можна пропустити, якщо код вже на сервері)
- ✅ OpenAI API ключ
- ✅ LiveAvatar конфігурація (API key, Avatar ID, Voice ID, Context ID)
- ✅ LiveKit конфігурація (URL, API Key, API Secret)
- ✅ Cloudflare SSL режим (Flexible або Full)

### 5. Готово!

Додаток буде доступний на: **https://teacherdemo.site**

---

## Що робить скрипт автоматично

✅ Встановлює Node.js 20.x
✅ Встановлює nginx, PostgreSQL, Redis
✅ Створює базу даних PostgreSQL
✅ Генерує безпечні паролі та секрети
✅ Створює .env файл з вашою конфігурацією
✅ Білдить фронтенд та бекенд
✅ Налаштовує nginx з WebSocket підтримкою
✅ Запускає додаток через PM2
✅ Налаштовує автозапуск при перезавантаженні
✅ Налаштовує firewall

---

## Альтернатива: Деплой через GitHub

Якщо ваш код на GitHub:

```bash
# На сервері
cd /tmp
wget https://raw.githubusercontent.com/YOUR_REPO/main/deploy.py
sudo python3 deploy.py
```

Скрипт сам клонує репозиторій і зробить деплой.

---

## Оновлення додатку

```bash
cd /var/www/teacher-agent

# Якщо через git
git pull

# Білд
cd web && npm run build
cd ../server && npm run build

# Перезапуск
pm2 restart teacher-agent
```

---

## Troubleshooting

### Помилка "скрипт потрібно запускати з правами root"
```bash
sudo python3 deploy.py
```

### PM2 не запустився
```bash
pm2 logs teacher-agent  # Подивитися помилки
pm2 restart teacher-agent
```

### Nginx помилка
```bash
nginx -t  # Перевірка конфігурації
systemctl restart nginx
```

### База даних не працює
```bash
systemctl status postgresql
systemctl restart postgresql
```

---

## ⚠️ ВАЖЛИВО

Після деплою **обов'язково**:
1. Видаліть GitHub токен: https://github.com/settings/tokens
2. Створіть новий токен для наступного разу
3. Перевірте, що домен teacherdemo.site вказує на IP 157.230.104.129 в Cloudflare

---

## Корисні команди

```bash
# Статус додатку
pm2 status

# Логи додатку
pm2 logs teacher-agent

# Логи nginx
tail -f /var/log/nginx/teacher-agent-error.log

# Перезапуск nginx
systemctl restart nginx

# Перезапуск PostgreSQL
systemctl restart postgresql

# Перезапуск Redis
systemctl restart redis
```

---

**Готово!** Після деплою відкрийте https://teacherdemo.site 🎉
